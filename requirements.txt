# YH API Testing Framework - Core Dependencies
# 核心依赖 - 最小化安装

# 测试框架核心
pytest>=7.0.0
requests>=2.28.0
PyYAML>=6.0
jsonpath-ng>=1.5.3
colorama>=0.4.6
requests-toolbelt>=1.0.0

# ============================================
# 可选依赖 - 按需安装
# ============================================

# 报告功能（安装命令：pip install -r requirements.txt -r requirements-reporting.txt）
# allure-pytest>=2.12.0
# jinja2>=3.1.0
# lxml>=4.9.0

# 文档服务器（安装命令：pip install api-test-yh-pro[docs]）
# fastapi>=0.104.0
# uvicorn>=0.24.0
# pydantic>=2.0.0

# 数据库支持（安装命令：pip install api-test-yh-pro[database]）
# pymysql>=1.0.2
# redis>=4.5.0

# WebSocket支持（安装命令：pip install api-test-yh-pro[socket]）
# websockets>=11.0.0
# paramiko>=3.0.0

# 数据处理（安装命令：pip install api-test-yh-pro[data]）
# faker>=19.0.0
# pandas>=2.0.0
# openpyxl>=3.1.0

# 完整功能（安装命令：pip install api-test-yh-pro[full]）
# 包含上述所有可选依赖

# ============================================
# 安装指南
# ============================================
# 1. 最小安装（仅核心功能）：
#    pip install -r requirements.txt
#
# 2. 使用pyproject.toml安装特定功能：
#    pip install api-test-yh-pro[reporting]  # 报告功能
#    pip install api-test-yh-pro[docs]       # 文档服务器
#    pip install api-test-yh-pro[database]   # 数据库支持
#    pip install api-test-yh-pro[full]       # 完整功能
#
# 3. 组合安装多个功能：
#    pip install api-test-yh-pro[reporting,docs,database]

